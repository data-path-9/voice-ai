import React from 'react';

export type ChatOutputNodeDetailProps = {
  children: React.ReactNode;
};

export function ChatOutputNodeDetail({ children }: ChatOutputNodeDetailProps) {
  return <>{children}</>;
}
