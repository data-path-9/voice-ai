import React from 'react';

export type ChatInputNodeDetailProps = {
  children: React.ReactNode;
};

export function ChatInputNodeDetail({ children }: ChatInputNodeDetailProps) {
  return <>{children}</>;
}
